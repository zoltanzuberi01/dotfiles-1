#############
# Autostart #
#############

nitrogen --restore &
tint2 -c ~/.config/tint2/OpenBox-Gruvbox-tint2rc &
picom &

# If u want to set gif as wallpapers:
# 1.dependencies: xwinwrap gifsicle
# 2.add path of your gif file to the following line
# NOTICE! gif file resolution should be the same as your monitor resolution 
# so you need to find out how to resize gifs using gifsicle

# xwinwrap -ni -fs -un -nf -b -ovr -- gifview -w WID [path/to/gif] -a &
